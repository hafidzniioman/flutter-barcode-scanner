import 'package:flutter/material.dart';

class DeleteScreenInventory extends StatelessWidget {
  const DeleteScreenInventory({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("Screen to delete item is under development")),
    );
  }
}
