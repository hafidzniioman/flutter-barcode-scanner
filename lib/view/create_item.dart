import 'package:flutter/material.dart';

class CreateScreenInventory extends StatelessWidget {
  const CreateScreenInventory({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("Screen to create item is under development")),
    );
  }
}
